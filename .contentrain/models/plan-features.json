{
  "id": "plan-features",
  "name": "Plan Features",
  "kind": "collection",
  "domain": "system",
  "i18n": false,
  "description": "Feature flags, usage limits, and overage pricing per plan",
  "fields": {
    "category": {
      "description": "Feature category for grouping in pricing UI",
      "options": [
        "ai",
        "cdn",
        "media",
        "forms",
        "api",
        "workflow",
        "roles",
        "enterprise"
      ],
      "required": true,
      "type": "select"
    },
    "community_value": {
      "description": "Community Edition value (AGPL-only self-host, no ee/ bridge). Typically 'true'/'unlimited' for core features and 'false' for ee/-dependent features.",
      "required": true,
      "type": "string"
    },
    "enterprise_value": {
      "description": "Enterprise plan value (number for limits, true/false for features, unlimited for Infinity)",
      "required": true,
      "type": "string"
    },
    "free_value": {
      "description": "Free plan value (number for limits, true/false for features)",
      "required": true,
      "type": "string"
    },
    "requires_ee": {
      "description": "Whether this feature requires the Enterprise Edition bridge (ee/) to function. When true, the feature is force-disabled in Community Edition regardless of plan-tier values.",
      "required": true,
      "type": "string"
    },
    "roadmap": {
      "description": "True when the feature appears in the plan matrix as marketing but has no runtime enforcement yet. UI surfaces show a 'Coming Soon' badge.",
      "type": "string"
    },
    "key": {
      "description": "Dot-notation key used in hasFeature() and getPlanLimit() (e.g. cdn.delivery, ai.messages_per_month)",
      "required": true,
      "type": "string",
      "unique": true
    },
    "name": {
      "description": "Human-readable feature or limit name",
      "required": true,
      "type": "string"
    },
    "overage_price": {
      "description": "Per-unit overage price in USD (null if not applicable)",
      "type": "number"
    },
    "overage_unit": {
      "description": "Overage billing unit (e.g. per message, per GB, per submission)",
      "type": "string"
    },
    "overage_settings_key": {
      "description": "JSONB key used inside workspaces.overage_settings to toggle this overage (e.g. ai_messages, cdn_bandwidth). Required when overage_price is set.",
      "type": "string"
    },
    "pro_value": {
      "description": "Pro plan value",
      "required": true,
      "type": "string"
    },
    "sort_order": {
      "description": "Display order within category",
      "required": true,
      "type": "integer"
    },
    "starter_value": {
      "description": "Starter plan value",
      "required": true,
      "type": "string"
    },
    "type": {
      "description": "Whether this entry is a boolean feature flag or a numeric limit",
      "options": [
        "feature",
        "limit"
      ],
      "required": true,
      "type": "select"
    }
  }
}
